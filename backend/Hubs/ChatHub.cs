using Microsoft.AspNetCore.SignalR;

namespace GcmsSoc.API.Hubs
{
    public class ChatHub : Hub
    {
        // Hub methods can be empty because we broadcast messages from our controllers.
        // However, if we need to track connected users or do other real-time actions, we can do it here.
    }
}
